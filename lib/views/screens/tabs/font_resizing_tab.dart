import 'package:flutter/material.dart';
import 'package:flutter_app/views/screens/settings/font_resizing.dart';

class FontResizingTab extends StatelessWidget {
  const FontResizingTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return FontResizing();
  }
}
