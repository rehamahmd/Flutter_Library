import 'package:flutter_app/enums/enums.dart';

class GalleryModel {
  String? title;
  String? url;
  GalleryType? type;
  GalleryModel({required this.title, required this.url, required this.type});
}
